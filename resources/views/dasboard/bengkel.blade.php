
<div>
    <h1>Data Module</h1>
    <ul>
        @foreach ($module as $key => $value)
            <li>{{ $key }}: {{ $value }}</li>
        @endforeach
    </ul>
</div>

<div>
    <h1>Data User</h1>
    <ul>
        @foreach ($user as $key => $value)
            <li>{{ $key }}: {{ $value }}</li>
        @endforeach
    </ul>
</div>


<div>
    <h1>Data User</h1>
    <ul>
        @foreach ($user as $key => $value)
            <li>{{ $key }}: {{ $value }}</li>
        @endforeach
    </ul>
</div>


<div>
    <h1>Data User</h1>
    <ul>
        @foreach ($user as $key => $value)
            <li>{{ $key }}: {{ $value }}</li>
        @endforeach
    </ul>
</div>

